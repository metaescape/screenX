# Screen, Audio, gif Recorder and Image Capture

A simple Python script to record screen, audio, and save as gif or video. It can also capture screenshots.

## Requirements

for audio:

```
sudo apt-get install portaudio19-dev
sudo apt-get install pavucontrol
```

for video:

```
sudo apt-get install ffmpeg
```

```
pip install pyaudio mss
```
